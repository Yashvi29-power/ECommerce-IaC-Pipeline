# Week 3 - Day 2

---

## Tasks Performed

- Initialized the Terraform working directory using `terraform init`.
- Formatted Terraform configuration files using `terraform fmt`.
- Validated the Terraform configuration using `terraform validate`.
- Generated an execution plan using `terraform plan`.
- Saved the execution plan using `terraform plan -out=tfplan`.
- Reviewed the proposed infrastructure changes before deployment.

---


---

## Commands Executed

```bash
terraform init
terraform fmt
terraform validate
terraform plan
terraform plan -out=tfplan
```

---

